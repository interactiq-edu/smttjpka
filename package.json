{
  "name": "interactiq",
  "version": "0.1.0",
  "private": true,
  "packageManager": "npm@11.19.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:web": "npm run dev --workspace=@interactiq/web",
    "dev:worker": "npm run dev --workspace=@interactiq/worker",
    "build": "npm run build --workspace=@interactiq/contracts && npm run build --workspace=@interactiq/web && npm run build --workspace=@interactiq/worker",
    "typecheck": "npm run build --workspace=@interactiq/contracts && npm run typecheck --workspaces --if-present",
    "test": "npm run test --workspaces --if-present"
  }
}
